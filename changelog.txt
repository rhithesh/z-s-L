

Known Issues:
1. llm-Hallucination issue(high priority)
2. disabling system logs as mediapipe keeps throwing its logs solution use a 
 downgraded version of mediapipe(low priority). Not usefull as this will be replaced by 
 Sima ai Gstreamer pipeline.



fixed issue:
1. _main_play_audio  is now implemented as a 
parentClass  method making sure piper_audio_queue is exclusive.
2.Fixed microphone issue  removed default_index=0 to None sd will fallback to default set by pactl.


features:
1. tool_calls passed to sima ai api directly(high).
2. closed loop for music and call.
3. voice and video(DMS) from Gstreamer pipeline.